import Foundation

enum Constants {
    enum AppIconName: String {
        case chevronRight = "chevron.right"
        case chevronLeft = "chevron.left"

    }

    enum Gen: String {
        case enabledNextButton = "Enabled next button"
    }

    enum ErrorMessage: String {
        case initNotImplemented = "init(coder:) has not been implemented"
    }
}
